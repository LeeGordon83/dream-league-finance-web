<div class="tab-pane fade" id="jackpot-summary" role="tabpanel" aria-labelledby="jackpot-summary-tab">
    <br/>
    <h4 style="text-align:center">Jackpot Summary</h4>
    <br/>
    <table class="table table-hover table-sm small">
        <tr>
            <th style="text-align:center">Week</th>
            <th style="text-align:center">Winners</th>
        </tr>
        {% for week in winnings.jackpotWinners %}
            <tr>
                <td style="text-align:center">{{ week.weekId }}</td>
                <td style="text-align:center">
                    {% for name in week.managerNames %}
                        {{ name }}
                        {% if not loop.last %}, {% endif %}
                    {% endfor %}
                </td>
            </tr>
        {% endfor %}

    </table>
</div>
