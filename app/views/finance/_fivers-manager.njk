<div class="tab-pane fade show active" id="fivers-manager" role="tabpanel" aria-labelledby="fivers-manager-tab">
    <br/>
    <h4 style="text-align:center">Fivers By Manager</h4>
    <br/>
      <table class="table table-hover table-sm small">
        <tr>
            <th style="text-align:center">Manager</th>
            <th style="text-align:center">Amount Won</th>
        </tr>
        {% for manager in winnings.fiversByManager %}
                <tr>
                    <td style="text-align:center">{{ manager.managerName }}</td>
                    <td style="text-align:center">
                    £{{ manager.count }}
                    </td>
                </tr>
        {% endfor %}

    </table>
</div>
